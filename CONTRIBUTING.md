Contributing to CoQuí
-----------------------------------------------

When contributing to CoQuí, please follow these steps:

1. **Fork** the CoQuí repository to your
   own GitHub account and make your changes there.
2. **Open all pull requests against the `develop` branch, not `main`.** The
   `main` branch is updated only from `develop` by the maintainers.
